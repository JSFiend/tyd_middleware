{
  "name": "tyd_middleware",
  "version": "1.1.0",
  "description": "think you do middleware",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint --ext .js ./",
    "lint-fix": "eslint --fix --ext .js ./",
    "pretest": "eslint --ext .js ./",
    "test": "nyc --reporter=html --reporter=text mocha",
    "coverage": "nyc report --reporter=text-lcov | coveralls"

  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JSFiend/tyd_middleware.git"
  },
  "author": "combinelee",
  "license": "ISC",
  "devDependencies": {
    "coveralls": "^3.0.4",
    "eslint": "^5.16.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.17.3",
    "mocha": "^6.1.4",
    "nyc": "^14.1.1"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "finalhandler": "^1.1.2"
  }
}
